import React from 'react';

const NewsPage = () => {
  return (
    <div>
      <h1>Сторінка новин</h1>
      <ul>
        <li>Новина 1: React Router v6 спрощує створення захищених маршрутів.</li>
        <li>Новина 2: Використання localStorage для авторизації є простим, але небезпечним для реальних проєктів.</li>
        <li>Новина 3: Хуки (hooks) змінили підхід до написання компонентів у React.</li>
      </ul>
    </div>
  );
};

export default NewsPage;